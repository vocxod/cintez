{{ header }}

<div id="information-newslist" class="container">
  <ul class="breadcrumb">
    {% for breadcrumb in breadcrumbs %}
    <li>
      <a href="{{ breadcrumb.href }}">{{ breadcrumb.text }}</a></li>
    {% endfor %}
  </ul>

  <div class="row">
    <div class="col-sm-12 ">
      
        <div class="col-sm-8 article_start">
            {#
            <br>
            {{is_new.image}}
            #}
            <h4 style="font-weight:bold;">{{ is_new.title }}</h4>
            <p>
              {{ is_new.description }}
            </p>
        </div>
       
        <div class="col-sm-4 ">
            <h4 class="h4-info"><spam class="green-uline">{{ heading_title_pub }}&nbsp;</span></h4>
            <br/>
            {{ toparticles }}
        </div>
    </div>
  </div> {# row 1 #}
</div>{# information-newslist #}



{{ footer }}

